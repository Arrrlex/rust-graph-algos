// Some simple algos to try implement:

// Path finding algos
//  depth-first
//  breadth-first
//  A*

// Graph generation (generating all graphs up to certain size)

use crate::representations::UndirectedGraph;

pub fn generate_graphs(n: usize) -> Vec<UndirectedGraph> {
    vec![]
}
